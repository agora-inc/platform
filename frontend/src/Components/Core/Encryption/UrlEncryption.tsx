import React, { Component } from "react";

interface Props {
  url: string;
}

export default class UrlEncryption extends Component<Props> {
  constructor(props: Props) {
    super(props);
    this.state 
  }

  render() {
      return ""
  }
}
